name: Update chart

on:
  workflow_call:
    inputs:
      RUN_MODE:
        description: >-
          Mode the workflow is run in. 'caller' to trigger the workflow in a
          remote chart repository, 'called' to update the chart in the current
          repository via a pull request, 'local' to bump the chart and commit
          directly on the current branch (monorepo pipelines - the new version
          is exposed as the 'chart-version' output).
        required: true
        type: string
      WORKFLOW_NAME:
        description: Name of the workflow to trigger in the chart repository (eg. 'update-chart.yml')
        required: false
        type: string
        default: update-app-version.yml
      CHART_REPO:
        description: Name of the chart repository (eg. 'this-is-tobi/helm-charts')
        required: false
        type: string
      CHART_DIR:
        description: Name of the folder containing the chart (in CHART_REPO)
        required: false
        type: string
        default: charts
      CHART_NAME:
        description: Name of the chart to update (in CHART_DIR)
        required: true
        type: string
      APP_VERSION:
        description: >-
          The app version to inject in Chart.yaml (appVersion). Leave empty to
          keep the current appVersion - a chart-only release where the chart
          version bumps on its own lifecycle without any app change.
        required: false
        type: string
        default: ""
      UPGRADE_TYPE:
        description: Should update 'major', 'minor', 'patch' or 'prerelease'
        required: false
        type: string
        default: patch
      PRERELEASE_IDENTIFIER:
        description: Prerelease identifier (only used if UPGRADE_TYPE is 'prerelease')
        required: false
        type: string
        default: rc
      AUTOMERGE_PRERELEASE:
        description: Automatically merge the PR when UPGRADE_TYPE is 'prerelease' (requires APP_CLIENT_ID/APP_PRIVATE_KEY or GH_PAT)
        required: false
        type: boolean
        default: false
      AUTOMERGE_RELEASE:
        description: Automatically merge the PR when UPGRADE_TYPE is not 'prerelease' (requires APP_CLIENT_ID/APP_PRIVATE_KEY or GH_PAT)
        required: false
        type: boolean
        default: false
      AUTOMERGE_METHOD:
        description: >-
          How the chart update pull request is merged when automerge is enabled.
          `auto` queues it and lets GitHub merge once required status checks
          pass - this needs "Allow auto-merge" enabled on the repository.
          `admin` merges immediately, bypassing branch protection and required
          status checks. Prefer `auto`; use `admin` only deliberately.
        required: false
        type: string
        default: auto
      BASE_BRANCH:
        description: Base branch to open the chart-update pull request against
        required: false
        type: string
        default: main
      RUNS_ON:
        description: Runner labels as JSON array (e.g., '["ubuntu-24.04"]' or '["self-hosted", "linux"]')
        required: false
        type: string
        default: '["ubuntu-24.04"]'
    secrets:
      APP_CLIENT_ID:
        description: >-
          GitHub App Client ID (e.g. Iv23li..., NOT the numeric App ID). Supply
          with APP_PRIVATE_KEY to authenticate as a GitHub App.
          Takes precedence over GH_PAT. Required (with APP_PRIVATE_KEY) or
          GH_PAT in caller mode, and for automerge in any mode.
          See docs/05-authentication.md.
        required: false
      APP_PRIVATE_KEY:
        description: GitHub App private key (PEM). Required alongside APP_CLIENT_ID.
        required: false
      GH_PAT:
        description: >-
          GitHub Personal Access Token. Legacy alternative to
          APP_CLIENT_ID/APP_PRIVATE_KEY; still supported, but App authentication is
          preferred.
        required: false
    outputs:
      chart-version:
        description: New chart version computed by the bump (called and local modes)
        value: ${{ jobs.called.outputs.chart-version }}
      previous-chart-version:
        description: Chart version before the bump (called and local modes)
        value: ${{ jobs.called.outputs.previous-chart-version }}
      commit-sha:
        description: SHA of the bump commit pushed on the current branch (local mode only)
        value: ${{ jobs.called.outputs.commit-sha }}

jobs:
  caller:
    name: Update chart
    runs-on: ${{ fromJSON(inputs.RUNS_ON) }}
    permissions: {}
    if: ${{ inputs.RUN_MODE == 'caller' }}
    env:
      # `secrets` is not available in a step-level `if`, but it is here.
      HAS_APP_AUTH: ${{ secrets.APP_CLIENT_ID != '' && secrets.APP_PRIVATE_KEY != '' }}
      # Exactly one of the two set: a typo or a half-finished setup. Never a
      # valid configuration, and silently falling back would downgrade auth.
      HAS_PARTIAL_APP_AUTH: ${{ (secrets.APP_CLIENT_ID != '') != (secrets.APP_PRIVATE_KEY != '') }}
    steps:
    - name: Validate inputs
      env:
        CHART_REPO: ${{ inputs.CHART_REPO }}
        AUTOMERGE_METHOD: ${{ inputs.AUTOMERGE_METHOD }}
      run: |
        set -euo pipefail

        if [ "$HAS_PARTIAL_APP_AUTH" = "true" ]; then
          echo "::error::APP_CLIENT_ID and APP_PRIVATE_KEY must be supplied together, but only one was. Continuing would silently fall back to GH_PAT or GITHUB_TOKEN instead of authenticating as the App."
          exit 1
        fi

        # Validated here rather than at the dispatch, so a malformed value fails
        # before a token is minted for it.
        if [ -z "$CHART_REPO" ]; then
          echo "::error::CHART_REPO is required when RUN_MODE is 'caller'"
          exit 1
        fi

        if ! printf '%s' "$CHART_REPO" | grep -Eq '^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$'; then
          echo "::error::CHART_REPO must be 'owner/repository', got '$CHART_REPO'. A value without a slash would mint a token for an owner named after the repository."
          exit 1
        fi

        case "$AUTOMERGE_METHOD" in
          auto|admin) ;;
          *)
            echo "::error::AUTOMERGE_METHOD must be 'auto' or 'admin', got '$AUTOMERGE_METHOD'"
            exit 1
            ;;
        esac

    # CHART_REPO is "owner/repo", validated above. The owner is taken from it
    # rather than from github.repository_owner so the chart repo may live under
    # another account. Neither value is a secret, so GITHUB_OUTPUT is fine here
    # - the token itself must never go there.
    - name: Resolve chart repository coordinates
      id: chart-repo
      if: ${{ env.HAS_APP_AUTH == 'true' }}
      env:
        CHART_REPO: ${{ inputs.CHART_REPO }}
      run: |
        set -euo pipefail
        echo "owner=${CHART_REPO%%/*}" >> "$GITHUB_OUTPUT"
        echo "name=${CHART_REPO##*/}" >> "$GITHUB_OUTPUT"

    # Scoped to the chart repository and to `actions: write` only - this token
    # dispatches a workflow and does nothing else.
    - name: Generate GitHub App token
      id: app-token
      if: ${{ env.HAS_APP_AUTH == 'true' }}
      uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
      with:
        client-id: ${{ secrets.APP_CLIENT_ID }}
        private-key: ${{ secrets.APP_PRIVATE_KEY }}
        owner: ${{ steps.chart-repo.outputs.owner }}
        repositories: ${{ steps.chart-repo.outputs.name }}
        permission-actions: write

    - name: Trigger helm-charts update
      env:
        GH_TOKEN: ${{ steps.app-token.outputs.token || secrets.GH_PAT }}
        CHART_DIR: ${{ inputs.CHART_DIR }}
        CHART_REPO: ${{ inputs.CHART_REPO }}
        WORKFLOW_NAME: ${{ inputs.WORKFLOW_NAME }}
        CHART_NAME: ${{ inputs.CHART_NAME }}
        APP_VERSION: ${{ inputs.APP_VERSION }}
        UPGRADE_TYPE: ${{ inputs.UPGRADE_TYPE }}
        PRERELEASE_IDENTIFIER: ${{ inputs.PRERELEASE_IDENTIFIER }}
        AUTOMERGE_PRERELEASE: ${{ inputs.AUTOMERGE_PRERELEASE }}
        AUTOMERGE_RELEASE: ${{ inputs.AUTOMERGE_RELEASE }}
        AUTOMERGE_METHOD: ${{ inputs.AUTOMERGE_METHOD }}
      run: |
        set -euo pipefail

        if [ -z "$GH_TOKEN" ]; then
          echo "::error::Caller mode dispatches a workflow in another repository, which GITHUB_TOKEN cannot do. Pass APP_CLIENT_ID/APP_PRIVATE_KEY (preferred) or GH_PAT."
          exit 1
        fi

        # Remove trailing slashes
        while [ "${CHART_DIR%/}" != "$CHART_DIR" ]; do CHART_DIR="${CHART_DIR%/}"; done

        echo "Trigger workflow '$WORKFLOW_NAME' in repository '$CHART_REPO' to update chart '$CHART_DIR/$CHART_NAME' with app version '$APP_VERSION'"

        DISPATCH_ARGS=(
          -f "APP_VERSION=$APP_VERSION"
          -f "CHART_NAME=$CHART_NAME"
          -f "CHART_DIR=$CHART_DIR"
          -f "UPGRADE_TYPE=$UPGRADE_TYPE"
          -f "PRERELEASE_IDENTIFIER=$PRERELEASE_IDENTIFIER"
          -f "AUTOMERGE_PRERELEASE=$AUTOMERGE_PRERELEASE"
          -f "AUTOMERGE_RELEASE=$AUTOMERGE_RELEASE"
          -f RUN_MODE=called
        )

        # AUTOMERGE_METHOD is newer than this dispatch contract. A chart
        # repository whose entry-point workflow predates it does not declare the
        # input, and `gh workflow run` rejects the whole dispatch with 422
        # "Unexpected inputs provided" rather than ignoring the extra value - so
        # sending it unconditionally would break every existing chart repo.
        # Try with it, fall back without it, and say which happened. The first
        # attempt dispatches nothing when it fails, so the retry is safe.
        if dispatch_output=$(gh workflow --repo "$CHART_REPO" run "$WORKFLOW_NAME" \
          "${DISPATCH_ARGS[@]}" -f "AUTOMERGE_METHOD=$AUTOMERGE_METHOD" 2>&1); then
          printf '%s\n' "$dispatch_output"
          echo "Dispatched with AUTOMERGE_METHOD=$AUTOMERGE_METHOD"
          exit 0
        fi

        case "$dispatch_output" in
          *"Unexpected inputs provided"*)
            echo "::warning::'$WORKFLOW_NAME' in '$CHART_REPO' does not declare an AUTOMERGE_METHOD input, so the chart repository's own default applies instead of '$AUTOMERGE_METHOD'. Add the input to that workflow to control the merge method from here - see docs/52-update-helm-chart.md."
            gh workflow --repo "$CHART_REPO" run "$WORKFLOW_NAME" "${DISPATCH_ARGS[@]}"
            ;;
          *)
            printf '%s\n' "$dispatch_output" >&2
            exit 1
            ;;
        esac

  called:
    name: Update chart
    runs-on: ${{ fromJSON(inputs.RUNS_ON) }}
    permissions:
      contents: write
      pull-requests: write
    if: ${{ inputs.RUN_MODE == 'called' || inputs.RUN_MODE == 'local' }}
    outputs:
      chart-version: ${{ steps.update-chart.outputs.NEXT_VERSION }}
      previous-chart-version: ${{ steps.update-chart.outputs.CURRENT_VERSION }}
      commit-sha: ${{ steps.push-local.outputs.COMMIT_SHA }}
    env:
      # `secrets` is not available in a step-level `if`, but it is here - so the
      # presence check is hoisted to the job and the step tests `env`.
      HAS_APP_AUTH: ${{ secrets.APP_CLIENT_ID != '' && secrets.APP_PRIVATE_KEY != '' }}
      # Exactly one of the two set: a typo or a half-finished setup. Never a
      # valid configuration, and silently falling back would downgrade auth.
      HAS_PARTIAL_APP_AUTH: ${{ (secrets.APP_CLIENT_ID != '') != (secrets.APP_PRIVATE_KEY != '') }}
    steps:
    - name: Validate inputs
      env:
        AUTOMERGE_METHOD: ${{ inputs.AUTOMERGE_METHOD }}
      run: |
        set -euo pipefail

        if [ "$HAS_PARTIAL_APP_AUTH" = "true" ]; then
          echo "::error::APP_CLIENT_ID and APP_PRIVATE_KEY must be supplied together, but only one was. Continuing would silently fall back to GH_PAT or GITHUB_TOKEN instead of authenticating as the App."
          exit 1
        fi

        case "$AUTOMERGE_METHOD" in
          auto|admin) ;;
          *)
            echo "::error::AUTOMERGE_METHOD must be 'auto' or 'admin', got '$AUTOMERGE_METHOD'"
            exit 1
            ;;
        esac

    # Only 'called' mode consumes this token, for the pull request and the
    # merge. 'local' mode pushes with the checkout credential on purpose (see
    # the checkout step below), so minting here would hand the job a
    # write-capable credential nothing uses.
    # Scoped to this repository only: passing `owner:` without `repositories:`
    # would widen the token to every repo in the installation.
    - name: Generate GitHub App token
      id: app-token
      if: ${{ env.HAS_APP_AUTH == 'true' && inputs.RUN_MODE == 'called' }}
      uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
      with:
        client-id: ${{ secrets.APP_CLIENT_ID }}
        private-key: ${{ secrets.APP_PRIVATE_KEY }}
        permission-contents: write
        permission-pull-requests: write

    # Deliberately NOT given the App token. The 'local'/'called' push below
    # targets the branch the caller's CD pipeline triggers on; a credential that
    # can trigger workflows would re-enter that pipeline. The commit also
    # carries [skip ci] as the primary, token-independent guard - see the push
    # step. Do NOT add `token:` to this step.
    - name: Checks-out repository
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

    - name: update chart version
      id: update-chart
      env:
        CHART_DIR: ${{ inputs.CHART_DIR }}
        CHART_NAME: ${{ inputs.CHART_NAME }}
        APP_VERSION: ${{ inputs.APP_VERSION }}
        UPGRADE_TYPE: ${{ inputs.UPGRADE_TYPE }}
        PRERELEASE_IDENTIFIER: ${{ inputs.PRERELEASE_IDENTIFIER }}
      run: |
        # Remove trailing slashes
        while [ "${CHART_DIR%/}" != "$CHART_DIR" ]; do CHART_DIR="${CHART_DIR%/}"; done

        if [ ! -f "$CHART_DIR/$CHART_NAME/Chart.yaml" ]; then
          echo "Chart.yaml not found in $CHART_DIR/$CHART_NAME"
          exit 1
        fi

        echo "Update chart '$CHART_NAME' in $CHART_DIR/$CHART_NAME with app version '$APP_VERSION'"
        CURRENT_VERSION=$(yq '.version' "$CHART_DIR/$CHART_NAME/Chart.yaml")

        # Bump version using Release Please compatible logic
        if [[ "$UPGRADE_TYPE" == "prerelease" ]]; then
          # Parse version: extract base version and prerelease part
          if [[ $CURRENT_VERSION =~ ^([0-9]+)\.([0-9]+)\.([0-9]+)(-(.+))?$ ]]; then
            MAJOR="${BASH_REMATCH[1]}"
            MINOR="${BASH_REMATCH[2]}"
            PATCH="${BASH_REMATCH[3]}"
            PRERELEASE="${BASH_REMATCH[5]}"

            if [[ -z "$PRERELEASE" ]]; then
              # No prerelease: bump patch then add prerelease identifier
              # This prepares the next version (e.g., 0.2.0 -> 0.2.1-rc)
              NEXT_VERSION="${MAJOR}.${MINOR}.$((PATCH + 1))-$PRERELEASE_IDENTIFIER"
            else
              # Has prerelease: extract current identifier
              if [[ "$PRERELEASE" =~ ^([a-z]+)(\.([0-9]+))?$ ]]; then
                CURRENT_IDENTIFIER="${BASH_REMATCH[1]}"
                CURRENT_NUM="${BASH_REMATCH[3]}"

                if [[ "$CURRENT_IDENTIFIER" != "$PRERELEASE_IDENTIFIER" ]]; then
                  # Different identifier: switch to new identifier without number
                  # e.g., 1.0.0-alpha.2 -> 1.0.0-beta
                  NEXT_VERSION="${MAJOR}.${MINOR}.${PATCH}-$PRERELEASE_IDENTIFIER"
                elif [[ -z "$CURRENT_NUM" ]]; then
                  # Same identifier without number: add .1
                  # e.g., 1.0.0-rc -> 1.0.0-rc.1
                  NEXT_VERSION="${MAJOR}.${MINOR}.${PATCH}-$PRERELEASE_IDENTIFIER.1"
                else
                  # Same identifier with number: increment
                  # e.g., 1.0.0-rc.1 -> 1.0.0-rc.2
                  NEXT_NUM=$((CURRENT_NUM + 1))
                  NEXT_VERSION="${MAJOR}.${MINOR}.${PATCH}-$PRERELEASE_IDENTIFIER.${NEXT_NUM}"
                fi
              else
                echo "Unexpected prerelease format: $PRERELEASE"
                exit 1
              fi
            fi
          else
            echo "Invalid version format: $CURRENT_VERSION"
            exit 1
          fi
        else
          # Standard version bump (major, minor, patch)
          if [[ $CURRENT_VERSION =~ ^([0-9]+)\.([0-9]+)\.([0-9]+)(-(.+))?$ ]]; then
            MAJOR="${BASH_REMATCH[1]}"
            MINOR="${BASH_REMATCH[2]}"
            PATCH="${BASH_REMATCH[3]}"
            PRERELEASE="${BASH_REMATCH[5]}"

            if [[ -n "$PRERELEASE" ]]; then
              # Has prerelease: release the official version without prerelease
              NEXT_VERSION="${MAJOR}.${MINOR}.${PATCH}"
            else
              # No prerelease: perform standard version bump
              case "$UPGRADE_TYPE" in
                major)
                  NEXT_VERSION="$((MAJOR + 1)).0.0"
                  ;;
                minor)
                  NEXT_VERSION="${MAJOR}.$((MINOR + 1)).0"
                  ;;
                patch)
                  NEXT_VERSION="${MAJOR}.${MINOR}.$((PATCH + 1))"
                  ;;
                *)
                  echo "Invalid upgrade type: $UPGRADE_TYPE"
                  exit 1
                  ;;
              esac
            fi
          else
            echo "Invalid version format: $CURRENT_VERSION"
            exit 1
          fi
        fi

        echo "Update chart from version '$CURRENT_VERSION' to '$NEXT_VERSION'"

        echo "Update chart files..."
        # Empty APP_VERSION = chart-only release: the chart version bumps on
        # its own lifecycle and the current appVersion is left untouched
        if [ -n "$APP_VERSION" ]; then
          sed -i "s/^appVersion: .*/appVersion: $APP_VERSION/" "$CHART_DIR/$CHART_NAME/Chart.yaml"
        fi
        sed -i "s/^version: .*/version: $NEXT_VERSION/" "$CHART_DIR/$CHART_NAME/Chart.yaml"

        echo "Generate chart documentation..."
        docker run \
          --rm \
          --volume "$(pwd)/$CHART_DIR/$CHART_NAME:/helm-docs" \
          -u "$(id -u)" \
          docker.io/jnorwood/helm-docs:latest

        {
          echo "CURRENT_VERSION=$CURRENT_VERSION"
          echo "NEXT_VERSION=$NEXT_VERSION"
          echo "BRANCH_NAME=$CHART_NAME-v$NEXT_VERSION"
        } >> "$GITHUB_OUTPUT"

    - name: Commit and push chart bump
      id: push-local
      if: ${{ inputs.RUN_MODE == 'local' }}
      env:
        CHART_DIR: ${{ inputs.CHART_DIR }}
        CHART_NAME: ${{ inputs.CHART_NAME }}
        NEXT_VERSION: ${{ steps.update-chart.outputs.NEXT_VERSION }}
      run: |
        set -euo pipefail

        CHART_DIR="${CHART_DIR%/}"
        git config user.name 'github-actions[bot]'
        git config user.email 'github-actions[bot]@users.noreply.github.com'

        git add "$CHART_DIR/$CHART_NAME"
        # [skip ci] is the primary loop guard, and the only one that does not
        # depend on which credential ends up pushing: GitHub skips push- and
        # pull_request-triggered runs when the head commit message contains it.
        # This push targets the branch the caller's CD pipeline triggers on, so
        # without it an App-authenticated push would re-enter CD, which would
        # release again, which would bump the chart again - and because each
        # cycle produces a fresh commit, nothing converges. Releasing the chart
        # in the same run using this workflow's outputs is the intended flow.
        # The `chore(chart):` prefix is a third guard: release-please does not
        # cut a release from it. Keep all three.
        git commit -m "chore(chart): release $CHART_NAME $NEXT_VERSION [skip ci]"

        # Tolerate concurrent pushes from other jobs of the same pipeline
        git pull --rebase origin "$GITHUB_REF_NAME"
        git push origin "HEAD:$GITHUB_REF_NAME"

        echo "COMMIT_SHA=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

    - name: Create pull request
      id: create-pr
      if: ${{ inputs.RUN_MODE == 'called' }}
      uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1 # v8.1.1
      with:
        # App token, then GH_PAT, then GITHUB_TOKEN. The first two can trigger
        # workflows, so the chart update pull request actually gets CI; under
        # GITHUB_TOKEN it never did.
        token: ${{ steps.app-token.outputs.token || secrets.GH_PAT || github.token }}
        branch: ${{ steps.update-chart.outputs.BRANCH_NAME }}
        base: ${{ inputs.BASE_BRANCH }}
        title: "${{ inputs.APP_VERSION != '' && format('Update chart {0} app to v{1}', inputs.CHART_NAME, inputs.APP_VERSION) || format('Update chart {0} to v{1}', inputs.CHART_NAME, steps.update-chart.outputs.NEXT_VERSION) }}"
        add-paths: |
          **/**
        commit-message: "${{ inputs.APP_VERSION != '' && format('chore: update chart {0} app to v{1}', inputs.CHART_NAME, inputs.APP_VERSION) || format('chore: update chart {0} to v{1}', inputs.CHART_NAME, steps.update-chart.outputs.NEXT_VERSION) }}"
        draft: false
        body: |
          🤖 This PR has been automatically generated, please check updates before merging.

          - Chart version has been updated from `${{ steps.update-chart.outputs.CURRENT_VERSION }}` to `${{ steps.update-chart.outputs.NEXT_VERSION }}`
          ${{ inputs.APP_VERSION != '' && format('- App version has been updated to `{0}`', inputs.APP_VERSION) || '- App version has been left unchanged (chart-only release)' }}
          - Chart documentation has been regenerated

    - name: Automerge chart update PR
      if: ${{ ((inputs.AUTOMERGE_PRERELEASE && inputs.UPGRADE_TYPE == 'prerelease') || (inputs.AUTOMERGE_RELEASE && inputs.UPGRADE_TYPE != 'prerelease')) && steps.create-pr.outputs.pull-request-number }}
      env:
        GH_TOKEN: ${{ steps.app-token.outputs.token || secrets.GH_PAT }}
        # Repository metadata is read with the built-in token, which always has
        # access to its own repo - a narrowly scoped PAT or App token may not.
        METADATA_TOKEN: ${{ github.token }}
        GITHUB_REPOSITORY: ${{ github.repository }}
        PR_NUMBER: ${{ steps.create-pr.outputs.pull-request-number }}
        # Already validated by the 'Validate inputs' step at the top of this job.
        AUTOMERGE_METHOD: ${{ inputs.AUTOMERGE_METHOD }}
      run: |
        set -euo pipefail

        # Automerge is gated on the AUTOMERGE_* inputs above, never on whether a
        # credential happens to be present. Supplying APP_CLIENT_ID must not
        # silently switch merging on, and a caller that asked to merge and
        # silently did not has a broken release - so this fails loudly.
        if [ -z "$GH_TOKEN" ]; then
          echo "::error::Automerge is enabled but no credential was supplied. Pass APP_CLIENT_ID/APP_PRIVATE_KEY (preferred) or GH_PAT."
          exit 1
        fi

        echo "Merging PR #$PR_NUMBER using method '$AUTOMERGE_METHOD'"

        if [ "$AUTOMERGE_METHOD" = "admin" ]; then
          echo "::warning::Force-merging PR #$PR_NUMBER, bypassing branch protection and required status checks (AUTOMERGE_METHOD=admin)"
          gh pr merge "$PR_NUMBER" --rebase --admin
          exit 0
        fi

        # `--auto` needs "Allow auto-merge" on the repository. Fail with a
        # pointed message rather than silently falling back to --admin, which
        # would merge past the very checks the App token now makes run.
        automerge_allowed=$(GH_TOKEN="$METADATA_TOKEN" gh api "repos/$GITHUB_REPOSITORY" --jq '.allow_auto_merge')
        if [ "$automerge_allowed" != "true" ]; then
          echo "::error::AUTOMERGE_METHOD=auto requires 'Allow auto-merge' in Settings > General on '$GITHUB_REPOSITORY'. Enable it, or set AUTOMERGE_METHOD=admin to force-merge past branch protection."
          exit 1
        fi

        gh pr merge "$PR_NUMBER" --rebase --auto
